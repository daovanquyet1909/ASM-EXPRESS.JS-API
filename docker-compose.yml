version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      MONGODB_URI: mongodb+srv://daovanquyet1909:Vanquyet1909@asm.hywxo9f.mongodb.net/nodejs?retryWrites=true&w=majority
      ENV: production
    depends_on:
      - db
  db:
    image: mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: daovanquyet1909
      MONGO_INITDB_ROOT_PASSWORD: Vanquyet1909
    ports:
      - "27017:27017"
